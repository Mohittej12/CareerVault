{
  "buildCommand": "cd build-and-blend-cv && npm install && npm run build",
  "outputDirectory": "build-and-blend-cv/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
